本冒烟包用于快速验证：
- 后端 API 可启动并挂载 /api 全量路由（3000）
- Admin 前端可开发访问（9000, 代理到 3000）
- Shop 前端可开发访问（5173, 代理到 3000）
- 数据库可初始化并种入测试数据，重置到 SKU 库存模式

如需生产部署：
- 请使用 CI 构建前端静态资源（pnpm build），并在后端用 Nginx 提供静态文件和反向代理 /api
- 将 NODE_ENV=production，并准备生产级别 MySQL、高可用、日志/监控等
